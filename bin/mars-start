#!/usr/bin/env node

var mars = require('../lib/mars')
  , string = require('string')
  , cl = require('cl')
  , colors = require('colors')

mars.checkGitDirExistsSync()

var HEAD = mars.getHeadSync()
  , head = HEAD.substr(0,7)

mars.goto(HEAD, 0, function(err, prev, cur) {
  if (err) cl.exit(100, err)

  console.log('Moving to beginning: %s', cur.shortName.bold.green)
})

